# --- core ---
Django>=5.2,<5.3
psycopg[binary]>=3.2,<4.0
django-vite>=3.0,<4.0
Pillow>=11.0,<12.0

# --- API for React islands ---
djangorestframework>=3.15,<4.0

# --- background jobs (COI recalculation, image resizing, reindexing) ---
celery[redis]>=5.4,<6.0
redis>=5.0,<7.0

# --- production server ---
gunicorn>=23.0,<24.0
